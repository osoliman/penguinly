server {
    listen 80;
    server_name _;  # Replace with your domain or Lightsail static IP

    # Redirect HTTP → HTTPS (uncomment once you have a cert)
    # return 301 https://$host$request_uri;

    client_max_body_size 16M;

    # Static files served directly by nginx
    location /static/ {
        alias /home/ubuntu/penguinly/static/;
        expires 30d;
        add_header Cache-Control "public, immutable";
        gzip_static on;
    }

    # Proxy everything else to gunicorn
    location / {
        proxy_pass         http://127.0.0.1:8000;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
        proxy_connect_timeout 10s;
    }
}

# HTTPS block — enable after running: sudo certbot --nginx -d yourdomain.com
# server {
#     listen 443 ssl http2;
#     server_name yourdomain.com;
#
#     ssl_certificate     /etc/letsencrypt/live/yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/yourdomain.com/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;
#
#     client_max_body_size 16M;
#
#     location /static/ {
#         alias /home/ubuntu/penguinly/static/;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location / {
#         proxy_pass         http://127.0.0.1:8000;
#         proxy_set_header   Host              $host;
#         proxy_set_header   X-Real-IP         $remote_addr;
#         proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#         proxy_read_timeout 120s;
#     }
# }
